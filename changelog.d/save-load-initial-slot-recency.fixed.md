- **Save/Load screen:** Now opens with the cursor on whichever slot was
  saved most recently (scrolled into view), matching a reference
  implementation's own save-screen startup, not independently confirmed
  against genuine RPG_RT under wine. Previously it always opened on slot 1 regardless of
  which slot actually held the newest save.
