- **WOLF RPG Editor (ウディタ/Woditor)** `Effect`(290)'s Picture-target
  `Shake` now nudges a picture by (dx, dy) a given number of times,
  toggling every N frames and always settling back to center before
  stopping, the same per-frame-ticked animation shape `SwitchFlicker`
  already established. See
  `docs/adr/0085-wolf-rpg-editor-effect-picture-shake.md`.
