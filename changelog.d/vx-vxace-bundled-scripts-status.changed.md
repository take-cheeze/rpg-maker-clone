- `docs/TODO.md`'s VX/VX Ace "Run the bundled scripts" entry was still marked
  🚧 even though every one of its own sub-bullets — including "A real test
  bed," the last one still open as of two cycles ago — had already been
  checked off ✅, and `docs/rpgvx-rgss-api-gap.md`'s own summary already
  concludes all seven of its sections are done. Flipped the top-level marker
  to match, with a short note pointing at the gap doc's one remaining caveat
  (a specific real VX Ace release's custom `Game_Temp` defeating the CLI-flag
  battle probe — a test-tooling limit on reaching that release's own custom
  battle entry point, not a gap in the engine). Doc-only, no code change.
