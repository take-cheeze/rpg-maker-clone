- **Android:** an on-screen FPS/CPU readout (LVGL's perf monitor, enabled only
  there in `include/lv_conf.h` via `LV_USE_SYSMON`/`LV_USE_PERF_MONITOR`) sits
  in the window's top-right corner, clear of the virtual gamepad's buttons.
  Desktop keeps the monitors off.
