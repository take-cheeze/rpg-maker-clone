- `docs/TODO.md`'s "Not fixed this cycle — confirmed bugs, left for a
  dedicated pass" marker (the デフォ戦botまとめ battle-trivia triage's own
  punch list) was still 🚧 even though every sub-item under it had since
  been fixed across cycles #233-#236, or closed as a genuine design
  difference rather than a bug (the skill/item target binding to a
  `Combatant` object, not a slot index). Flipped to ✅ with a note on where
  each item landed. Doc-only, no code change.
