- **RPG2000 field Item screen** geometry re-measured on genuine RPG_RT.exe
  under wine: the list box now fills the screen below the description banner
  (not content-sized), lays its two columns out as 144px cells pitched 160px
  apart, draws each count as `:` plus a right-aligned figure at the cell's
  right edge, scrolls a row at a time once the bag outgrows 12 rows (with the
  windowskin's blinking up/down arrows on the box's frame edges), and the
  actor-target panel butts "Lv"/"HP"/"MP" straight against their figures.
