- Moved the CORS proxy Worker under `scripts/cors-proxy/`, the shared Wio
  mruby I/O gem under `app/wio/hal-wio-io/`, and PPSSPP patches into the root
  `patches/` directory. Updated build, CI, deployment, and documentation paths.
