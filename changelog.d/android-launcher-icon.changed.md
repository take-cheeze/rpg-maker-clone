- **Android:** the launcher icon is the project's own Sapphire Chip mark
  (`assets/logo/sapphire-chip-512.png` downscaled into every
  `app/android/app/src/main/res/mipmap-*` density), replacing SDL's default
  asset.
