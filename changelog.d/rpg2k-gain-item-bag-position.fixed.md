- A newly gained RPG Maker 2000/2003 item is now placed in the bag where
  `RPG_RT.exe` places it — immediately before the first held item with a larger
  id, appended when there is none — instead of always being appended. Measured
  against a genuine `RPG_RT.exe` under wine by buying three different weapons
  into a deliberately out-of-order bag.
