- **WOLF RPG Editor (ウディタ/Woditor)** `LoadVariable`(221)/
  `SaveVariable`(222) — "セーブデータからの読み込み"/"セーブデータへの書
  き込み" — now read and write a single variable or string into a small
  per-save-slot file (`Save/SaveDataNN.sav`, or a name given directly by a
  string variable), independent of the full whole-game save/load (`220`,
  still unimplemented). See
  `docs/adr/0082-wolf-rpg-editor-save-variable.md`.
