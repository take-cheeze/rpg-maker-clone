- **Status screen:** The "Next" EXP figure now shows the absolute EXP
  threshold for the next level -- matching a reference implementation's own
  status-drawing and EXP-string formatting, not independently confirmed
  against genuine RPG_RT under wine -- instead of the
  remaining delta to reach it (e.g. "2000" rather than "766" for an actor
  1234 EXP into a 2000 threshold).
