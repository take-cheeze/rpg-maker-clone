- The build workflow now publishes the whole-program bc2cpp coverage report
  in its job summary instead of tracking `docs/bc2cpp_coverage.txt`, avoiding
  generated-file conflicts between bc2cpp pull requests.
